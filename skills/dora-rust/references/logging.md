# ロギングと可視化

- 制御モジュールをpublisher、独立ロガーをsubscriberとして分離する。
- 完全記録用経路と、表示用の間引き・最新値経路を必要に応じて分ける。
- 入力から列や描画系列への変換を純粋関数にし、保存・描画を副作用へ閉じ込める。
- CSVやdatの先頭に列名と単位を書き、時刻と値の対応を固定する。
- 生成周期、受信周期、保存周期、描画周期を区別する。
- 終了時flushと書き込み失敗を呼び出し側が判断できる形で表す。
- GUI表示は数値変換テストで代用せず、対象環境で短時間確認する。
