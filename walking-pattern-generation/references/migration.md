# ROS 2/C++からの移植

- 公開API、状態、初期値、更新順、終了条件、callback周期を対応表へ整理する。
- ROS 2のmessage型を計算コアへ持ち込まず、境界で意味のある値型へ変換する。
- QoS、timestamp、frame ID、名前と配列値の対応を通信境界の契約として保持する。
- C++の暗黙変換、整数除算、添字、未初期化値、初期化順に依存する挙動を無条件に写さない。
- 代表入力を固定し、最終出力だけでなく途中の状態遷移も移植元と比較する。
- 既知の欠陥を再現しない場合は、差異、終了条件、呼び出し側への影響を記録する。
