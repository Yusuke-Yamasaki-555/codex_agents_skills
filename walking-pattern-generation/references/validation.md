# 検証

- 静止条件、左右対称性、連続性、有限値、支持領域などモデルの不変条件を確認する。
- 初期・終端時刻、期待サンプル数、step境界の前後、最小入力を確認する。
- 非有限値、負やゼロの周期、不正な高さ、欠落、重複、収束失敗を契約に応じて拒否する。
- 同じ入力と初期状態に対する出力の決定性を確認する。
- 固定値回帰は移植互換性に使い、不変条件テストの代用にしない。
- 数値差が意図したアルゴリズム変更か、演算順や精度による退行かを区別する。

純粋関数の単体テストと、通信、モデル、GUIを必要とする統合確認を分ける。
