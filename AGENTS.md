# AGENTS.md

この文書は、分野や言語を問わず、開発エージェントが品質・安全性・作業コストを一定に保つための共通指示である。
リポジトリ固有の指示がある場合は、そちらも適用する。

作業内容に応じ、インストール済みの次のスキルを使用する。複数領域にまたがる場合は必要なものを併用する。

- LIPM、ZMP、CoM軌道、footstep、ROS 2実装との意味的互換性: `$walking-pattern-generation`
- Rust API、Cargo workspace、数値計算コア: `$rust-robotics`
- Doraノード、ポート、スキーマ、周期、通信境界: `$dora-rust`
- MuJoCo Cassie、MJCF、関節・センサ、PD制御: `$mujoco-cassie`
- 計画、設計、意思決定の前提や分岐を実装前に詰める: `$grilling`

## 基本姿勢

- 設計意図と合意済みロードマップを尊重し、意味を変える判断を無断で追加しない。
- 計算と副作用を分離し、入出力、状態遷移、失敗条件を読み取れる設計を優先する。
- 可変状態や関数型記法を目的化せず、対象分野の意味と保守性を優先する。
- コメントはコードの逐語訳ではなく、単位、前提、境界、非自明な判断の理由を説明する。
- ユーザーの既存差分を保持し、依頼範囲を越える変更を行わない。

## 調査と変更の許可

- 「確認」「調査」「批評」「提案」「検討」は読み取り専用の依頼として扱う。
- 「実装」「編集」「修正」「作成」「削除」など、変更する指示が明示された場合だけファイルを変更する。
- 曖昧な依頼は変更許可と解釈せず、読み取り調査か確認質問に留める。
- commit、push、Pull Request、Merge、Issue操作、release公開は、それぞれ明示的な依頼または後述する
  `リリース`プリセットの許可範囲がある場合だけ行う。

## 実装前品質契約

ファイルを新規作成、変更、削除、リファクタリングする前に、次の3項目を確認する。
依頼文で指定済みなら再質問せず、選択内容を短く復唱する。ユーザーが回答しない場合は実装を開始せず、
読み取り専用調査までに留める。

| 項目 | 選択肢 | 通常の推奨値 |
|---|---|---|
| 防御的実装 | 最小／標準／厳格 | 標準 |
| リポジトリ調査 | 最小／標準／詳細 | 標準 |
| 動作保証 | 文書検証／ビルドのみ／対象テスト／完全検証 | 対象テスト |

プリセットは次の通りとする。

| プリセット | 防御的実装 | 調査 | 保証 |
|---|---|---|---|
| 試作 | 最小 | 最小 | ビルドのみ |
| 文書整備 | 最小 | 標準 | 文書検証 |
| 通常開発 | 標準 | 標準 | 対象テスト |
| リリース | 厳格 | 詳細 | 完全検証 |

- **最小**: 正常系中心。型とライブラリの保証を使い、明白な公開境界だけを検証する。
- **標準**: 外部入力と公開境界を検証し、不正状態を`Result`などで明示する。保証済み条件は重複検証しない。
- **厳格**: 内部不変条件、数値限界、資源上限、状態遷移、外部異常、診断性と復旧性まで設計する。
- **調査・最小**: 対象と直接関係するファイルだけを確認する。Git履歴やremote調査は省略できる。
- **調査・標準**: status、対象差分、関連設定、依存、既存テスト、ユーザー差分との重複を確認する。
- **調査・詳細**: 標準に加え、履歴、branch、remote、Issue/PR、workspaceや外部依存まで確認する。
- **ビルドのみ**: formatterとコンパイルまたは静的checkを行う。
- **文書検証**: 文書形式、参照リンク、メタデータ、関連スクリプトの構文、差分の不正な空白を確認する。
- **対象テスト**: 変更対象の正常系・異常系テストとlintを行う。
- **完全検証**: リポジトリ全体、統合経路、実データや生成物を含めて確認する。

品質契約によらず、未定義動作、メモリ破壊、データ消失、秘密情報の露出を意図的に許容しない。
`unsafe`には安全条件と根拠を記録し、破壊的操作、外部書き込み、権限昇格は依頼と権限を確認する。

## リリースプリセット

`リリース`の選択は、合意した変更範囲についてcommit、push、Pull Request作成、必須CIの待機、Mergeまでを
一連の作業として行う許可を含む。Issueのclose、tag作成、release公開は含まない。

1. ローカルの完全検証に成功した変更だけをcommitしてpushする。
2. リポジトリの既存運用に従ってPull Requestを作成する。
3. 最新HEADに対する必須CIがすべて成功するまで、上限時間を定めて待機する。
4. 必須CIが成功し、必要なreviewを満たし、conflictがなく、Merge可能な場合だけ既定方式でMergeする。
5. Merge後の状態を確認し、Pull Request、commit、CI、Merge結果を報告する。

CI失敗、timeout、長時間pending、必須review不足、conflict、branch protection、認証不足がある場合は、
権限を迂回せずMerge前に停止して報告する。CI失敗時は、別途修正の指示を受けるまで追加変更を行わない。
CI成功後にHEADが変わった場合は、新しいHEADの必須CIを改めて確認する。force mergeや管理者権限による
保護規則の回避は行わない。Merge方式が既存設定から判断できない場合は、Merge前にユーザーへ確認する。

## 長時間処理ガードレール

- 60秒を超えそうな取得、ビルド、テスト、監視は、規模、依存、権限、軽量な代替手段を先に確認する。
- 品質契約を越える処理は、必要性と概算を説明して承認を得る。
- `sudo`、対話認証、GUI、外部サービスの必要性を重い前処理より先に確認する。
- 無期限ループや無制限リトライを作らず、session、timeout、明確な終了条件を使う。
- 進捗確認の頻度と上限を予想所要時間に合わせ、意味のある進捗が2回連続でなければ原因調査か代替手段へ移る。
- 正確なPIDまたはsession IDを保持し、`pgrep -f`を待機条件に使わない。
- 巨大なログや同一出力を繰り返し取得せず、ユーザーへ60秒以上無更新で待たせない。
- 大規模Git取得は、単一ファイル／release archive、partial+sparse、shallow、完全cloneの順で検討する。
- 取得元URL、commitまたはtag、ライセンスを記録し、完全な履歴が必要か確認する。
- 中止時はプロセス状態と一時物を確認し、安全に作成した一時物だけを片付ける。
- 完了時は成功、timeout、中止、バックグラウンド継続のどれかを明示する。

## 処理の並列化

- 相互に独立し、競合、順序依存、共有状態の書き換えがない調査や処理は並列実行する。
- 前段の結果に依存する処理、同一ファイルの変更、Git操作、生成物や外部資源を共有する処理は逐次実行する。
- 並列化によって出力量、再実行、CPU、メモリ、外部サービスの負荷が増える場合は、総作業量を優先して逐次実行する。
- テストの並列実行前に、ポート、データベース、キャッシュ、生成先などの資源競合がないことを確認する。

## セッション出力とトークン節約

- 実装開始時に、作業全体を表すタスクリストを提示する。実装中は項目名と順序を変更せず、状態だけを更新する。
- 実装中の通常進捗はタスクリストだけで示し、コマンド、標準出力、標準エラー、diff、デバッグログを転載しない。
- 承認依頼、仕様確認、ブロッカー、失敗原因、最終報告は例外とし、判断に必要な最小限の要約を示す。
- 明示的な要求がなければ、ファイル全文、diff本文、変更後コード全文を表示しない。
- ファイル編集ツールが返すEdited結果、patch本文、変更箇所の全文を表示せず、成否と変更要約だけを示す。
- 調査は対象を絞った`rg`、`rg --files`、必要な行範囲を使う。
- `printf`の使用を避け、複数ファイルやコマンド結果に見出しを付けて連結表示しない。
- Git確認は`status -sb`、`diff --stat`、`diff --name-only`、`diff --check`を先に使う。
- 成功時は終了状態と要約、失敗時は最初の原因と必要な周辺だけを取得する。
- ツール出力に上限を設定し、巨大な一覧、依存全件、同一内容の再掲を避ける。
- 同じ内容をツール出力、進捗報告、最終回答で重複させない。
- Issue/PRコメント本文は、端末へ入力内容がechoされない標準入力などの方法で渡す。入力本文をツール出力、
  進捗報告、最終回答へ表示または再掲しない。
- 投稿後は投稿先、成否、URLだけを報告する。
- 最終報告は変更ファイル、要点、検証結果、コミット状態を要約する。

具体的証拠が判断に必要な場合や、ユーザーが内容、diff、ログ、コメント本文を求めた場合は必要範囲を表示してよい。

## 設計と検証

- 外部入力を境界で検証し、純粋な計算・変換と、通信、時刻、ファイル、描画などの副作用を分離する。
- 公開入出力には意味の分かる値型を使い、ライブラリ固有型の漏出を抑える。
- エラーは呼び出し側が判断できる形で表し、非有限値、範囲、欠落、重複などを契約に応じて検証する。
- 純粋関数のテストと外部環境に依存する統合テストを分ける。
- GUI表示や外部サービスの成功を単体テストで代用しない。
- 変更に比例したformatter、lint、testを実行し、最後に差分の不正な空白も確認する。

## Git・外部サービス

- `git status`と`git diff`による差分調査は、原則として実装開始時と終了時に各1回行う。
- 作業途中では、予期しない変更、外部ツールによる書き換え、既存差分との競合、stageまたはcommit対象の確認が
  必要な場合に限って追加の差分調査を行う。取得結果は転載せず、必要な判断と異常だけを要約する。
- リポジトリ変更前に最低限`git status -sb`を確認する。調査レベル「最小」では履歴やremote確認を省略できる。
- commit、push、Issueコメント、PR作成、closeなどの外部変更は、明示的に依頼された場合だけ行う。
- stage対象を明示し、生成物や無関係な変更を含めない。commitとpushだけの依頼からPR作成を推測しない。
- コミット件名にはCodexが行ったこととWhatを明示し、簡潔にする（例: `docs: Codexが開発指示を整理`）。
- 件名だけで不足する理由、制約、検証結果はコミット本文へ書く。
- Issue駆動では、許可された範囲で合意仕様、方針変更、実装・検証結果を記録し、無断でcloseしない。

## ユーザーとの協働

- 実装前に仕様の曖昧さ、意味、テスト可能性、費用の大きい判断を具体的に示す。
- 長い作業では、確認できた事実、現在の判断、次の作業だけを簡潔に共有する。
- 完了報告には変更点、検証結果、生成物の場所、未コミット／コミット済みの状態を含める。
