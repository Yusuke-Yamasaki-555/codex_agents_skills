# AGENTS.md

この文書は、対象分野やプログラミング言語を問わず、Codexなどのエージェントが一貫した
品質・安全性・作業コストでソフトウェア開発を行うための共通指示である。

タスクがRust計算コア、MuJoCo Cassie、Dora、ロギング、GitHub Issue運用に関係する場合は、
`wpg-rust-robotics/SKILL.md`の実践手順も読む。

## 基本姿勢

- ユーザーと設計意図を議論し、合意したロードマップに沿って段階的に実装する。
- 関数型プログラミングの考え方を重視し、計算を「入力値から出力値への変換」として表現する。
- 純粋な計算コアと、ファイル、描画、時刻、Dora、MuJoCoなどの副作用境界を分離する。
- 可変状態を機械的に排除しない。時間発展など必要な状態は小さな値型へまとめ、遷移の意味を
  読み取れるようにする。
- `map`や`scan`の使用自体を目的にしない。数式、物理的意味、状態遷移が明確になる書き方を
  優先する。
- Rustや使用ライブラリの初学者が読めるよう、単位、座標系、値の意味、非自明な判断を
  コメントとdoc commentで説明する。
- コメントはコードの逐語訳ではなく、「なぜこの境界・式・検証が必要か」を説明する。

## 実装前品質契約

コードの新規実装、既存コードの変更、リファクタリングを依頼された場合は、作業開始前に
次の3項目を必ずユーザーへ確認する。ユーザーが依頼文ですでに3項目または後述のプリセットを
指定している場合は、重ねて質問せず、理解した品質契約を短く復唱する。

1. 防御的実装レベル: `最小`／`標準`／`厳格`
2. リポジトリ調査レベル: `最小`／`標準`／`詳細`
3. 動作保証レベル: `ビルドのみ`／`対象テスト`／`完全検証`

確認前に、コード編集、依存追加、長時間ビルド、テスト実行を開始しない。仕様確認のための
短い読み取り専用調査は行ってよい。品質契約によって作業時間や外部処理が大きく変わる場合は、
その違いも質問時に簡潔に示す。

### 防御的実装レベル

#### 最小

- 正常系を中心に実装する。
- 型システムとライブラリの保証を活用する。
- 明らかに必要な公開境界だけを検証する。
- 細分化したエラー型や重複した`if`を追加しない。

試作、動作確認、一時的なコードに使用する。

#### 標準

- 外部入力と公開APIの境界を検証する。
- 必要に応じて非有限値、範囲、長さ、欠落、重複を確認する。
- 不正状態を`Result`などで明示する。
- 内部で保証済みの不変条件を何度も検証しない。

通常開発の推奨値とする。

#### 厳格

- 公開・内部境界の不変条件を詳細に検証する。
- エラーを原因別に分類する。
- オーバーフロー、数値精度、リソース上限、状態遷移、外部ライブラリの異常値を確認する。
- 必要に応じて復旧方法、診断情報、失敗時の原子性も設計する。

実機制御、安全性が重要な処理、長期運用するライブラリに使用する。

### リポジトリ調査レベル

#### 最小

- 対象ファイルと直接関係するコードだけを確認する。
- Git状態の詳細調査を省略する。

独立した新規ファイルやGit管理外の試作に使用する。

#### 標準

- `git status -sb`、対象差分、関連ファイル、設定、既存テストを確認する。
- ユーザーの未コミット変更と作業対象の重複を確認する。

通常のリポジトリ内変更の推奨値とする。

#### 詳細

- status、diff、log、branch、remoteを確認する。
- rebase、merge、stash、submodule、workspace、ignore、依存関係を確認する。
- 関連Issue、PR、移植元、既知の欠陥まで調査する。

commit・push、大規模リファクタリング、複数リポジトリをまたぐ変更に使用する。

### 動作保証レベル

#### ビルドのみ

- formatterとコンパイルまたは静的checkを行う。
- 単体テスト、実行確認、GUI確認を省略する。

#### 対象テスト

- 変更対象の単体テストと代表的な正常系・異常系を実行する。
- lintを実行し、必要に応じて短時間の動作確認を行う。

通常の機能追加の推奨値とする。

#### 完全検証

- workspaceまたはリポジトリ全体のテストとlintを実行する。
- integration test、実モデル、外部ファイル、GUI、ファイル出力などを必要に応じて確認する。
- 生成物の形式、件数、内容と`git diff --check`まで確認する。

### 品質プリセット

ユーザーは3項目を個別指定する代わりに、次のプリセットを指定できる。

| プリセット | 防御的実装 | リポジトリ調査 | 動作保証 |
|---|---|---|---|
| 試作 | 最小 | 最小 | ビルドのみ |
| 通常開発 | 標準 | 標準 | 対象テスト |
| リリース | 厳格 | 詳細 | 完全検証 |

### 選択できない安全性の下限

品質契約にかかわらず、次は必ず守る。

- 意図的に未定義動作、メモリ破壊、データ消失、秘密情報の露出を許容しない。
- `unsafe`を使う場合は安全条件を満たし、その根拠をコメントする。
- ユーザーの既存差分を無断で破棄・上書きしない。
- 破壊的操作、外部書き込み、権限昇格は依頼範囲と権限を確認する。

防御的実装レベルで選択するのは、不正入力や異常状態をどの範囲まで積極的に検出・分類するか
であり、未定義動作を許可するかどうかではない。

## 長時間処理ガードレール

ネットワーク取得、ビルド、テスト、パッケージ作成、ログ監視などが長時間化する可能性がある
場合は、次のルールをすべて適用する。

### 開始前

- 60秒を超えそうな処理は、開始前に規模、依存関係、権限、軽量な代替手段を確認する。
- 品質契約の範囲を超える長時間処理は、必要性と概算をユーザーへ説明して確認する。
- `sudo`、対話認証、GUI、外部サービスが必要かを、重い前処理より先に確認する。
- ダウンロードサイズや履歴全体が必要か不明なまま完全cloneを開始しない。

### 実行と待機

- 無期限の`while`、`until`、再帰的リトライを作らない。
- シェルの待機ループより、実行ツール固有のsession、wait、timeout機能を使用する。
- 長時間処理には明示的なtimeoutまたは終了条件を設定する。
- 同じ処理の進捗確認は原則3回までにする。
- 2回連続で意味のある進捗がない場合は停止し、原因調査または軽量な代替手段へ切り替える。
- `pgrep -f`を待機条件に使わない。検索コマンド自身へ一致して終了しない可能性がある。
- PIDを使う場合は、開始時に正確なPIDまたはsession IDを保持する。
- 進捗確認で巨大なログ、全ファイル一覧、同一出力を繰り返し取得しない。
- ユーザーを60秒以上無更新で待たせず、進捗と次の判断を簡潔に共有する。

### Git・外部ソース取得

大規模Gitリポジトリは、次の優先順位で取得する。

1. 必要な単一ファイルまたは公式release archive
2. `--filter=blob:none`とsparse-checkout
3. `--depth 1`などのshallow clone
4. 必要性を確認した上で完全clone

```bash
git clone --filter=blob:none --no-checkout <URL> <DIR>
git -C <DIR> sparse-checkout set <REQUIRED_PATH>
git -C <DIR> checkout <BRANCH_OR_TAG>
```

- submoduleでも履歴全体が不要ならshallow取得を検討する。
- ライセンスや再現性のために必要なcommit、tag、取得元URLを記録する。

### パッケージとソースビルド

旧バージョンやAURパッケージを扱う場合は、次の順で確認する。

1. システムのパッケージキャッシュ
2. AURヘルパーやビルドキャッシュ
3. 公式release archive
4. tag指定のshallow clone
5. 完全なソースミラーと再ビルド

- PKGBUILDの`git+https`が完全ミラーを取得する可能性を事前に確認する。
- 不足依存や`sudo`が必要なら、長いソース取得より先に解決する。
- 長時間処理を中止して別方式へ切り替えた場合は、残ったプロセスと一時ファイルを確認する。

### 完了・中止時

- 成功、timeout、中止、バックグラウンド継続のどれかを明確に確認する。
- 実行中プロセスを放置したまま同じ処理を重複起動しない。
- 中止時は、安全に作成した一時物だけを片付け、ユーザーの既存データを削除しない。
- 長時間化した原因と採用した代替手段を完了報告へ残す。

## セッション出力とトークン節約

ファイル調査、差分確認、ビルド、テストでは、セッションへ表示する出力量を必要最小限にする。
ツール出力もコンテキストを消費するため、確認できる情報を無条件に全文表示しない。

- ユーザーが明示的に求めない限り、ファイル全文をセッションへ表示しない。
- ユーザーが明示的に求めない限り、diff本文や変更後コード全文を表示しない。
- 実装前後のファイル内容を確認する場合は、必要な関数、行範囲、検索一致だけを取得する。
- ファイル探索では、対象を絞った`rg`、`rg --files`、行範囲指定を使用する。
- Git確認では、まず`git status -sb`、`git diff --stat`、`git diff --name-only`、
  `git diff --check`を使い、本文は問題箇所の調査が必要な場合だけ取得する。
- ビルドやテストでは、成功時に冗長な全ログを残さず、終了状態と要約を優先する。
- 失敗時もログ全体を繰り返し表示せず、最初の原因、関連箇所、必要な末尾だけを取得する。
- `git status --untracked-files=all`、巨大な`find`、依存関係全件、生成物一覧を無制限に表示しない。
- 同一内容を複数のコマンド、コメント、最終回答で重複して表示しない。
- GitHub IssueやPRへ投稿するコメント本文を、ツール入力、進捗報告、最終回答へ再掲しない。
- Issue・PRコメントの作成時は、本文を一時ファイルまたは標準入力経由で渡し、可能な限り
  セッションのツール表示へ露出させない。
- 投稿後は、投稿先、成功・失敗、コメントURLだけを簡潔に報告する。
- ツールには適切な出力上限を指定し、必要なら対象を分割して確認する。
- ユーザーへの進捗報告は、確認できた事実、現在の判断、次の作業だけを簡潔に伝える。

ただし、次の場合は必要な範囲を表示してよい。

- ユーザーがファイル内容、diff、ログ、コード例を明示的に求めた場合
- ユーザーがIssue・PRへ投稿したコメント本文の確認を明示的に求めた場合
- エラー原因や安全上の問題をユーザーが判断するために具体的証拠が必要な場合
- 変更前後の仕様差を短い抜粋で示すことが、説明コストを実質的に下げる場合

最終報告では、全文やdiffの再掲ではなく、変更したファイル、要点、検証結果、コミット状態を
要約する。

## 設計原則

### Functional core / imperative shell

次の方向へ責務を分ける。

```text
入力値
  → 検証
  → 純粋な正規化・制御・軌道計算
  → 出力値
  → MuJoCo更新／Dora通信／ファイル保存／描画
```

- 公開入出力は名前付きの値型で表し、ライブラリ固有型を外部APIへ漏らさない。
- エラーは`Result`と具体的なエラー型で明示する。
- 不正値、`NaN`、無限大、重複、欠落、単位・周期の不整合を境界で拒否する。
- X/Y、関節列、センサ列などの反復処理は、同じ意味の計算を共通化する。
- 数値互換性が必要な移植では、元実装の固定値回帰テストを残す。
- 元実装の欠陥を再現しない場合は、安全な終了条件と差異をdoc commentへ明記する。

### モジュール境界

- 計算ライブラリに描画、ロギング、Dora、ROS 2依存を持ち込まない。
- 描画・CSV・dat出力は専用クレートまたは専用モジュールへ置く。
- CLIの`main.rs`は、入力準備、ライブラリ呼び出し、副作用実行だけの薄い層にする。
- 将来のDora接続は、メッセージスキーマとポート名が未確定ならコメントアウトした境界例として
  残し、計算コアへ仮のDora型を混入させない。
- 複数クレートはCargo workspaceで管理し、ルートの`Cargo.lock`を使用する。

## MuJoCo・ロボット制御

- `mujoco-rs`とMuJoCo本体の対象バージョンを必ず一致させる。
- Arch LinuxのAUR版MuJoCoが`mujoco.pc`を提供しない場合は、workspaceの
  `.cargo/config.toml`で`MUJOCO_DYNAMIC_LINK_DIR=/usr/lib`を明示する。
- MJCFやアセットを無断で複製しない。ライセンスと上流履歴を保つ取得方法をユーザーと合意する。
- 現在のCassie運用では、`mujoco_menagerie`を独立cloneし、git sparse-checkoutで
  `agility_cassie`だけを展開する。ローカルcloneは本体リポジトリで追跡しない。
- 関節角度・角速度の入力は、状態の強制代入ではなく目標値として扱う。
- 目標角度・速度からmotor controlを求める制御則は純粋関数にする。
- motor controlはMJCFの`ctrlrange`内へ制限する。
- free jointやball jointを1自由度の能動関節として扱わない。
- 骨盤free jointは、関節列ではなく位置・クォータニオン姿勢として出力する。
- 関節limit、アクチュエータ、センサの名前・アドレス・次元はMJCFから取得し、ハードコードを
  最小限にする。
- 制御周期とシミュレーション周期を分離し、整数ステップ数を明示する。

## ロギング・可視化

- 制御データの取得周期と描画周期を分離できる設計にする。
- ログは制御をブロックしない境界へ置く。将来Doraを使う場合は独立ロガーノードを優先する。
- 解析用ログでは、先頭行に列名と単位を記載し、時刻と値の対応を固定する。
- 生成物は専用ディレクトリへ出力し、原則`.gitignore`対象にする。
- 描画の自動テストはディスプレイ環境に依存するため、数値テストと手動viewer確認を分離する。
- Wayland環境ではviewerを実際に短時間起動し、初期化、描画、状態同期にエラーがないことを確認する。

## テスト方針

- `lib.rs`を読みやすく保つため、単体テストは`src/test.rs`または`src/tests.rs`へ分離する。
- 純粋関数のテストと、実モデルを使うシミュレーションテストを分ける。
- 最低限、次を検証する。
  - 正常入力の固定値または物理的に妥当な応答
  - 不正値、非有限値、欠落、重複、周期不整合
  - 名前と内部アドレスの対応
  - 期待するサンプル数と時刻
  - 出力値が有限であること
  - 制御入力がlimit内であること
  - 目標入力に対し対象関節が妥当な方向へ変化すること
- 二足ロボットの暫定コントローラでは「転倒しないこと」を安易な合格条件にしない。
- GUIが表示されたことを単体テストで判定しない。viewerは手動または短時間の起動確認とする。
- 変更後は原則として次を実行する。

```bash
cargo fmt --all -- --check
cargo test --workspace
cargo clippy --workspace --all-targets --all-features -- -D warnings
cargo check --workspace
git diff --check
```

## Git・GitHub運用

- 作業開始前に`git status -sb`と既存差分を確認する。
- ユーザーの未コミット変更を保持し、自分の変更と区別する。
- ユーザーが明示するまでcommit、push、Issueコメント、PR作成を行わない。
- Issueを作業指示として使う場合は、開始前に合意済み仕様と計画をコメントする。
- 方針変更があればIssueへ追記し、古い説明との違いを明示する。
- 完了時は実装内容、テスト結果、コミット状態をIssueへコメントする。
- commit前に対象ファイルを明示的にstageし、生成物や無関係な変更を含めない。
- 日本語コミット名を求められた場合は、変更内容とCodexによる実装であることが分かる簡潔な
  メッセージを使う。
- ユーザーがcommitとpushだけを求めた場合、勝手にPRを作らない。

## ユーザーとの協働

- 実装前の確認では、仕様の曖昧さ、物理モデル上の意味、テスト可能性を具体的に指摘する。
- 合意後は、意味を変える判断を無断で追加しない。
- 長い作業では、節目ごとに進捗、確認できた事実、次の作業を簡潔に共有する。
- 完了報告には変更点、検証結果、生成物の場所、未コミット／コミット済みの状態を含める。
